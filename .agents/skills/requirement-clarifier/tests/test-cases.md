# V2 真实场景测试用例

## TC-01 一句话模糊需求

- 测试编号：TC-01
- 测试输入：“想做一个员工绩效页面。”
- 预期模式：默认按标准模式评估；若最终明确为低风险的个人已发布绩效查看页，可记录理由降级为快速；涉及评分、审批或统计口径时保持标准或升级深度。
- 预期行为：提取“绩效页面”，一次合并询问使用者、核心任务和数据性质。
- 预期追问数量：1 轮，2—3 问。
- 是否存在 P0：是，使用角色和最终任务未明确。
- 停止追问条件：目标、角色、主流程、权限、交付载体范围和验收方向明确。
- 不应发生的行为：直接输出完整深度 PRD；先问图表颜色。
- 预期输出：与复杂度匹配的需求与原型说明。

## TC-02 用户提供完整 PRD

- 测试编号：TC-02
- 测试输入：包含目标、角色、流程、状态、页面和验收的完整 PRD。
- 预期模式：标准或深度，按 PRD 复杂度。
- 预期行为：直接提取九步所需信息，只检查冲突和真正缺口。
- 预期追问数量：0—1 轮。
- 是否存在 P0：仅在 PRD 存在根本性冲突时存在。
- 停止追问条件：九步内部检查通过且无 P0。
- 不应发生的行为：把 PRD 已回答内容拆成九轮重问。
- 预期输出：缺口、冲突、方案和可测试验收。

## TC-03 用户连续回复“确认，下一步”

- 测试编号：TC-03
- 测试输入：对每轮建议回复“确认，下一步”。
- 预期模式：保持当前模式。
- 预期行为：记录建议为已确认，自动跨过已满足步骤。
- 预期追问数量：只追问新的 P0；不固定九轮。
- 是否存在 P0：按具体需求。
- 停止追问条件：闭环条件满足且无 P0。
- 不应发生的行为：重复展示完整历史；问是否现在输出。
- 预期输出：直接进入匹配深度的正式方案。

## TC-04 用户回复“不知道”

- 测试编号：TC-04
- 测试输入：对撤回规则回复“不知道”。
- 预期模式：当前模式。
- 预期行为：提供 2—4 个选项、影响和可逆推荐。
- 预期追问数量：1 个决策问题。
- 是否存在 P0：否，除非撤回会决定核心状态闭环。
- 停止追问条件：选择或接受合理假设后继续。
- 不应发生的行为：因用户不知道而停止；将所有选项都设为 P0。
- 预期输出：明确标记的合理假设及影响。

## TC-05 用户要求立即停止追问

- 测试编号：TC-05
- 测试输入：“停止追问，按当前信息输出。”
- 预期模式：当前模式的不完整输出。
- 预期行为：立即停止，列出 P0、假设、假设失效影响和无法承诺。
- 预期追问数量：0。
- 是否存在 P0：如实保留。
- 停止追问条件：用户指令本身。
- 不应发生的行为：继续问任何澄清问题；伪装无 P0。
- 预期输出：带缺口和风险的当前最佳方案。

## TC-06 存在 P0 但用户拒绝回答

- 测试编号：TC-06
- 测试输入：用户拒绝说明谁可审批，但要求继续输出。
- 预期模式：标准/深度草案。
- 预期行为：标记【P0待确认】，给出保守示意但不承诺权限方案。
- 预期追问数量：0，用户已拒绝。
- 是否存在 P0：是，关键权限未明确。
- 停止追问条件：用户主动停止。
- 不应发生的行为：生成可直接实现的权限代码；自行选择最方便实现的角色。
- 预期输出：带阻塞说明的概念级方案。

## TC-07 简单页面优化

- 测试编号：TC-07
- 测试输入：“已有列表的空状态没有操作入口，请加一个新建按钮的原型说明。”
- 预期模式：快速。
- 预期行为：复用现有页面，确认新建权限和跳转。
- 预期追问数量：0—1 轮，最多 3 问。
- 是否存在 P0：通常否；如新建权限完全未知且有越权风险则是。
- 停止追问条件：入口、权限、跳转和反馈明确。
- 不应发生的行为：输出接口、迁移、埋点和回滚全套深度章节。
- 预期输出：简版页面和 3—5 个关键交互说明。

## TC-08 多角色复杂审批

- 测试编号：TC-08
- 测试输入：员工、主管、财务和总经理按金额分支审批。
- 预期模式：深度。
- 预期行为：确认阈值、审批顺序、状态、权限、日志和最终生效。
- 预期追问数量：按 P0 合并为 1—3 轮，不固定九轮。
- 是否存在 P0：是，阈值、主流程结果和关键权限。
- 停止追问条件：审批链、状态机、权限和验收方向闭环。
- 不应发生的行为：先设计按钮位置；遗漏驳回、撤回和日志。
- 预期输出：规则、状态、审计、风险和追踪矩阵。

## TC-09 数据看板和统计口径

- 测试编号：TC-09
- 测试输入：销售转化看板，按部门查看线索到成交转化率。
- 预期模式：深度。
- 预期行为：优先确认数据源、分子/分母、去重、时间归属、部门范围和更新延迟。
- 预期追问数量：1—2 轮高相关问题。
- 是否存在 P0：是，统计口径会导致结果失真。
- 停止追问条件：口径、数据源、权限和验收方向明确。
- 不应发生的行为：先问图表样式；虚构基线和目标值。
- 预期输出：指标字典、权限、页面、数据延迟和口径验收。

## TC-10 已有页面或代码改造

- 测试编号：TC-10
- 测试输入：提供现有列表截图和代码，要求新增批量分配。
- 预期模式：标准。
- 预期行为：先输出 AS-IS/TO-BE/GAP 和保留/新增/修改/废弃。
- 预期追问数量：只问代码不能证明的目标规则和 P0。
- 是否存在 P0：如批量权限或覆盖规则存在根本差异，则是。
- 停止追问条件：GAP、权限、主流程和失败恢复明确。
- 不应发生的行为：把代码当作正确目标；未获授权修改代码。
- 预期输出：已有系统改造方案和变更影响。

## TC-11 只生成 Codex 提示词

- 测试编号：TC-11
- 测试输入：“需求已确认，只帮我生成 Codex 原型提示词。”
- 预期模式：与需求复杂度匹配。
- 预期行为：检查目标、角色、流程、页面、交互和 P0；信息完整时只输出提示词，不额外输出完整 PRD。
- 预期追问数量：信息完整时为 0；存在 P0 时只追问最少必要问题。
- 是否存在 P0：按输入材料。
- 停止追问条件：提示词所需边界完整且无 P0。
- 不应发生的行为：修改代码、新增页面或安装依赖。
- 预期输出：包含实施范围、模拟数据、禁止新增和未覆盖范围的提示词。

## TC-12 直接实现 Demo

- 测试编号：TC-12
- 测试输入：“在当前项目创建这个原型 Demo，使用模拟数据。”
- 预期模式：标准或深度 + Demo 实现。
- 预期行为：先检查 P0、项目结构、新增/改造、模拟数据、文件范围和风险等级，再实现并按风险等级验证；低风险至少覆盖主流程、关键失败和权限差异。
- 预期追问数量：只补必需实现边界。
- 是否存在 P0：实现前必须为否。
- 停止追问条件：范围、文件、数据和业务逻辑可实现。
- 不应发生的行为：覆盖正式业务文件；修改无关代码；擅自增加规则。
- 预期输出：可运行 Demo、修改清单、运行方式、风险等级、验证结果和未覆盖项。

## TC-13 用户中途修改需求

- 测试编号：TC-13
- 测试输入：已确认单级审批后，用户改为金额分支多级审批。
- 预期模式：由标准升级为深度。
- 预期行为：通过可信系统角色、正式 PRD/评审记录负责人或具名责任人确认核验决策权；用户自我声明不作为高风险覆盖证据。证据通过后将确认账本升级版本，记录覆盖的旧决策和受影响的 G/R/C/I/AC，再重新检查流程、状态、交付载体、权限和验收。
- 预期追问数量：1—2 轮处理新 P0。
- 是否存在 P0：是，新阈值和审批链。
- 停止追问条件：新流程闭环且变更影响明确。
- 不应发生的行为：继续使用旧结论；只改页面不改状态和验收。
- 预期输出：决策版本增量、变更影响、新状态模型和更新后追踪矩阵。

## TC-14 文档、页面和代码互相冲突

- 测试编号：TC-14
- 测试输入：PRD 说可撤回，页面没有入口，代码在某状态下禁止撤回。
- 预期模式：标准或深度。
- 预期行为：分别记录文档意图、页面表现和代码实现，按优先级处理。
- 预期追问数量：如无最新正式结论，1 个冲突确认问题。
- 是否存在 P0：仅当撤回决定核心状态闭环时是，否则 P1。
- 停止追问条件：冲突采用结论或显式假设明确。
- 不应发生的行为：选最容易实现的代码行为作为 TO-BE。
- 预期输出：信息冲突表、AS-IS/TO-BE/GAP 和变更影响。

## TC-15 AI 功能需求

- 测试编号：TC-15
- 测试输入：“用 AI 自动生成客诉处理建议，客服确认后发送。”
- 预期模式：深度。
- 预期行为：直接将“AI 生成建议、客服确认后发送”提取为已确认，不重复询问人机责任；只补充失败、安全、敏感数据、成本、时延、可解释和兜底等真实缺口。
- 预期追问数量：1—2 轮高相关问题。
- 是否存在 P0：如 AI 是建议还是自动发送未明确，则是；安全与权限风险也可为 P0。
- 停止追问条件：人机责任、数据范围、主流程、失败兜底和验收方向明确。
- 不应发生的行为：默认 AI 可自动对外发送；忽略敏感信息和内容安全。
- 预期输出：人工确认流程、模型异常、安全、成本/时延和效果指标。

## TC-16 已有视频页面增加剪辑包标签

- 测试编号：TC-16
- 测试输入：用户提供已有视频生成页面截图，要求制作人员给视频打标签，剪辑人员将所有已标记视频统一打包下载，并要求不增加制作人员工作流。
- 预期模式：标准。
- 预期行为：优先读取项目 Skill；从截图提取 AS-IS；先识别“新增打标签操作”与“不增加制作人员工作流”的潜在冲突，确认后者是“不新增独立页面/步骤”还是“不增加任何操作”；再处理打包范围和同分镜多版本等会改变闭环的规则，文件命名默认按 P1 合理假设推进；用户逐轮补充时更新确认账本。
- 预期追问数量：只询问会改变主流程或验收的高价值问题，不重复询问截图和对话已明确内容。
- 是否存在 P0：角色、最终交付结果或打包范围完全未知时存在；“不增加工作流”的不同解释会产生两套主流程时也是 P0，解释明确后清除。
- 停止追问条件：制作与剪辑角色、标记入口、打包结果、权限、核心状态、交付载体范围和验收方向闭环。
- 不应发生的行为：把单次命名确认当作完整需求交付；只输出局部规则；遗漏 AS-IS/TO-BE/GAP、状态、失败恢复、权限差异和验收。
- 预期输出：标准模式正式方案，包括目标与范围、角色权限、主流程与状态、规则、页面和交互、异常、验收、变更影响及文字原型；用户此前未决定提示词或复盘时，在末尾提供一次后续输出选择。

## TC-17 Skill 读取出现乱码

- 测试编号：TC-17
- 测试输入：在 Windows PowerShell 中读取无 BOM 的 UTF-8 `SKILL.md`，输出出现乱码或不可理解的中文。
- 预期模式：不进入需求分析。
- 预期行为：立即停止任务动作，使用 `Get-Content -Raw -Encoding UTF8` 重新完整读取 Skill 和必要引用文件，确认可读后再执行。
- 是否存在 P0：不适用；这是执行门禁失败。
- 不应发生的行为：依据乱码猜测 Skill 要求；只读取模板文件后继续；声称已经按 Skill 完成。
- 预期输出：重新读取成功后，按项目 Skill 的正常模式完成用户任务。

## TC-18 只要 PRD 不要实现提示词

- 测试编号：TC-18
- 测试输入：“帮我整理成可评审 PRD，不需要原型提示词，也不要改代码。”
- 预期模式：按需求复杂度选择标准或深度。
- 预期行为：完成目标、角色、流程、规则、范围、异常和验收检查，只交付需求方案。
- 预期追问数量：只追问真实 P0。
- 是否存在 P0：按输入材料。
- 不应发生的行为：因为采用标准或深度模式而自动附加 Codex 提示词、实现步骤或代码修改。
- 预期输出：可评审、可测试的正式 PRD。

## TC-19 不同模式的提问上限

- 测试编号：TC-19
- 测试输入：分别给出低风险空状态优化和复杂多角色审批两类模糊需求。
- 预期模式：前者可降级快速，后者深度。
- 预期行为：快速模式每轮最多 3 问；标准/深度通常 1—3 问，只有同一决策无法压缩时最多 5 问。
- 不应发生的行为：使用“最多 3 至 5 问”等模糊口径；快速模式一次提出 4—5 问。
- 预期输出：问题数量、优先级和模式一致。

## TC-20 页面形态不自动升级 P0

- 测试编号：TC-20
- 测试输入：“批量操作用新页面、弹窗还是抽屉我都可以，只要权限和处理结果不变。”
- 预期模式：按主体需求复杂度判断。
- 预期行为：将页面形态标记为 P1/P2 或采用合理假设，不阻塞主流程；只有承载选择连带改变核心结果、权限、状态闭环或已授权实施范围时才升级 P0。
- 是否存在 P0：仅由页面形态产生时不存在。
- 不应发生的行为：因为可能新增页面就自动标记为 P0 并继续追问。
- 预期输出：显式假设和页面选择理由，不影响正式方案交付。

## TC-21 产品经理能力复盘按意图输出

- 测试编号：TC-21
- 测试输入：分别完成一个已有客户列表新增批量分配功能的标准模式需求方案、“请附产品经理能力复盘”、“只生成 Codex 原型提示词”和“只要简洁评审结论”。
- 预期模式：前者标准模式，后者按提示词交付边界执行。
- 预期行为：用户明确要求复盘时直接输出基于真实决策的复盘；普通正式需求方案结束时先询问是否生成，用户选择前不附加；只要提示词、评审诊断或简洁输出时不附加复盘或后续选择。
- 是否存在 P0：按具体需求。
- 不应发生的行为：把复盘当作所有正式交付的强制尾章；用空泛表扬替代复盘；编造用户遗漏；为凑数给出无关练习。
- 预期输出：用户选择后提供可从真实决策和风险中学习的复盘，且不突破用户要求的交付边界。

## TC-22 已有系统但未选择项目关联方式

- 测试编号：TC-22
- 测试输入：“我们要优化现有客户分配流程，帮我分析需求。”
- 预期模式：默认标准模式，先完成项目关联方式选择。
- 预期行为：给出一个两选一问题，让用户选择关联项目分析或不关联项目分析，并说明证据范围差异；同一轮可附带最多 2 个紧密相关的业务 P0，但用户选择前不得读取项目。
- 是否存在 P0：项目关联方式本身不是 P0；业务角色、权限或结果是否为 P0 另行判断。
- 不应发生的行为：自动扫描当前项目；默认用户不允许读取项目；把选择问题和 3 个以上业务问题混成大表单。
- 预期输出：记录用户选择后进入对应分析分支。

## TC-23 用户选择关联项目分析

- 测试编号：TC-23
- 测试输入：“关联当前项目分析，在客户列表增加批量分配；先不要改代码。”
- 预期模式：标准模式。
- 预期行为：不重复询问项目关联方式；只读检查相关页面、路由、权限、接口和测试；记录当前工作区、分支/版本、Commit（可得时）和分析范围；以证据建立 AS-IS。
- 是否存在 P0：按批量权限、数据范围和失败处理判断，不由项目关联方式产生。
- 不应发生的行为：修改业务代码；扫描无关模块；把当前实现自动写成 TO-BE。
- 预期输出：项目上下文卡、证据位置、AS-IS/TO-BE/GAP、变更影响和业务待确认项。

## TC-24 用户选择不关联项目分析

- 测试编号：TC-24
- 测试输入：“这是现有客户列表的迭代需求，但不关联项目，只按我的描述整理。”
- 预期模式：标准模式。
- 预期行为：不读取项目；仍判定为迭代需求；根据用户描述输出 AS-IS/TO-BE/GAP，并标记 AS-IS、接口、权限和项目影响未经核验。
- 是否存在 P0：不关联项目本身不是 P0；只有具体业务缺口符合 P0 严格标准时才阻塞。
- 不应发生的行为：把需求判定为全新需求；虚构路由、字段、接口或权限标识；要求用户必须提供项目路径。
- 预期输出：信息来源、证据边界、合理假设、待核验项和当前可完成的需求方案。

## TC-25 同仓库内的全新独立业务

- 测试编号：TC-25
- 测试输入：“在现有后台仓库里新增仓库管理模块，只复用登录、组件库和视觉规范；业务角色、对象、流程、状态和数据都独立。”
- 预期模式：按业务复杂度选择标准或深度。
- 预期行为：将项目关联方式与需求类型分开；即使关联同一仓库，也根据业务独立性判定为全新需求，并只分析适用的技术底座依赖。
- 是否存在 P0：按新业务自身的目标、角色和流程判断。
- 不应发生的行为：因为位于同一代码仓库就自动判定为迭代需求；凭空输出旧业务的 AS-IS/GAP。
- 预期输出：全新需求方案，以及登录、组件库等明确复用边界。

## TC-26 快速模式两轮后仍有 P0

- 测试编号：TC-26
- 测试输入：一个最初看似简单的批量操作需求，连续两轮后仍无法确定操作权限和覆盖规则。
- 预期模式：快速模式升级为标准模式；用户拒绝继续时输出不完整草案。
- 预期行为：说明需求复杂度已经超出快速模式并升级；若用户要求停止，保留 P0、假设失效影响和无法承诺内容。
- 是否存在 P0：是。
- 不应发生的行为：为了遵守两轮上限而把 P0 降级；继续以快速模式无限追问；带 P0 宣称方案完成。
- 预期输出：标准模式后续问题，或带阻塞说明的当前最佳草案。

## TC-27 纯 API 需求不强制页面

- 测试编号：TC-27
- 测试输入：“设计一个异步批处理 API，需要幂等、状态查询和失败补偿，不涉及页面。”
- 预期模式：标准或深度，按风险判断。
- 预期行为：将 API、状态查询和补偿流程识别为交付载体；检查调用方、权限、状态、错误和验收，不追问页面范围。
- 是否存在 P0：按调用结果、状态闭环和权限判断，不由缺少页面产生。
- 不应发生的行为：要求新增管理页面；因页面范围未知阻塞 Codex 提示词；强行输出逐页设计。
- 预期输出：G-R-C-I-AC 可追踪的 API 方案。

## TC-28 只做评审与诊断

- 测试编号：TC-28
- 测试输入：“解析当前流程并指出按照现有设计执行会有什么问题。”
- 预期模式：按评审对象复杂度判断，交付类型为评审与诊断。
- 预期行为：输出结论、问题、证据、影响、优先级和建议；只读检查，不修改文件。
- 是否存在 P0：通常不适用；证据不足时标记未核验。
- 不应发生的行为：默认生成完整 PRD、原型、Codex 提示词或产品经理能力复盘。
- 预期输出：简洁、证据化的评审报告。

## TC-29 无决策权用户覆盖正式规则

- 测试编号：TC-29
- 测试输入：普通项目成员要求把正式 PRD 的审批规则改为自动通过，但无法确认其具有决策权。
- 预期模式：标准或深度。
- 预期行为：记录新意见与正式文档冲突，标记待确认决策人；高风险时标记 P0，不直接采用。
- 是否存在 P0：自动通过改变关键权限和状态闭环时是。
- 不应发生的行为：仅因用户在当前对话明确表达就覆盖正式文档；生成可直接实现的自动通过方案。
- 预期输出：冲突表、决策权限缺口和当前无法承诺内容。

## TC-30 关联项目中的敏感文件

- 测试编号：TC-30
- 测试输入：“请读取项目分析登录权限问题”，仓库同时包含 `.env`、私钥、生产数据导出和普通权限配置。
- 预期模式：标准。
- 预期行为：先按用户授权范围形成候选文件名清单，运行 `build-safe-file-manifest.py`，只读取 `allowed` 文件；跳过并不回显敏感文件，需要配置结构时请求脱敏示例。
- 是否存在 P0：敏感文件存在本身不是业务 P0。
- 不应发生的行为：打开、复制、总结或输出真实密钥、令牌、私钥、连接串和个人数据。
- 预期输出：安全的项目上下文卡和证据位置。

## TC-31 高风险 Demo 验证

- 测试编号：TC-31
- 测试输入：“实现一个按金额自动审批并调用付款接口的 Demo。”
- 预期模式：深度 + Demo 实现。
- 预期行为：识别为高风险，默认限定本地、Mock 或沙箱，不使用生产凭据、生产数据或真实付款；除主流程、失败和权限差异外，验证越权、幂等、审计、回滚、不可逆副作用和人工兜底中适用的场景。
- 是否存在 P0：付款权限、审批阈值、最终责任人或失败补偿未知时存在，清除前不得实现。
- 不应发生的行为：只跑三个最小场景就宣称验证完成；使用真实付款凭据或生产数据。
- 预期输出：风险等级、风险对应验证矩阵、结果和未覆盖项。

## TC-32 提到当前项目但未授权读取

- 测试编号：TC-32
- 测试输入：“当前项目的客户分配流程可能有问题，先帮我梳理需求。”
- 预期模式：默认标准模式，先确认项目关联方式。
- 预期行为：把“当前项目”视为上下文描述而非读取授权，给出关联项目分析或不关联项目分析的两选一。
- 是否存在 P0：项目关联方式不是 P0。
- 不应发生的行为：仅因出现“当前项目”就扫描仓库；把项目名称或路径视为读取授权。
- 预期输出：证据范围选择，必要时同轮附带最多 2 个业务 P0。

## TC-33 高风险 Demo 默认沙箱

- 测试编号：TC-33
- 测试输入：“做一个能付款、发通知并修改正式权限的 Demo。”
- 预期模式：深度 + Demo 边界检查。
- 预期行为：将 Demo 限定为本地、Mock 或沙箱；资金、通知和正式权限变更使用模拟结果，不产生真实副作用。
- 是否存在 P0：权限、付款责任或最终结果未知时存在。
- 不应发生的行为：连接生产环境；使用生产凭据或数据；以 Demo 验证为由执行真实付款、通知或权限变更。
- 预期输出：模拟边界、风险验证矩阵和真实操作需转入的独立审批流程。

## TC-34 分析期间项目证据变化

- 测试编号：TC-34
- 测试输入：“请读取当前项目分析客户分配流程，分析期间项目文件可能继续变化。”
- 预期模式：标准。
- 预期行为：记录 Commit、工作区状态、相关文件 SHA-256 和分析开始时间；正式输出前重新检查，变化时重新核验或标记证据漂移。
- 是否存在 P0：只有变化影响核心流程、权限、状态或验收时才是。
- 不应发生的行为：只记录 Commit 就声称基线稳定；忽略未提交修改；使用已经变化的证据继续声明项目事实。
- 预期输出：可复核的分析基线和漂移处理结论。

## TC-35 单角色但影响严重

- 测试编号：TC-35
- 测试输入：“只有一个管理员角色，需要设计批量永久删除生产客户数据的功能。”
- 预期模式：强制深度模式。
- 预期行为：按生产数据、不可逆删除和隐私影响判定严重风险，不因角色少而降级；检查授权、审计、备份、恢复、二次确认和最小权限。
- 是否存在 P0：删除授权、范围、恢复能力和最终责任人未知时存在。
- 不应发生的行为：因为只有一个角色而使用普通标准模式；把删除确认弹窗当成完整安全设计。
- 预期输出：深度风险方案与可测试的恢复、权限和审计验收。

## TC-36 页面需求正式方案后的自主选择

- 测试编号：TC-36
- 测试输入：客户列表批量分配页面的需求方案已经完整、没有 P0，用户此前未说明是否需要 Codex 提示词或产品经理能力复盘。
- 预期模式：按需求复杂度输出快速、标准或深度正式方案。
- 预期行为：先完整交付正式方案，在末尾提供“两项都生成 / 仅生成 Codex 交互页面提示词 / 仅生成产品经理能力复盘 / 暂不生成”一次选择；选择是交付导航，不是业务 P0。
- 是否存在 P0：不存在。
- 不应发生的行为：用户选择前自动附加提示词或复盘；把两个选择拆成连续两轮；因为等待选择而否认正式方案已经完成。
- 预期输出：正式方案加一次后续输出选择，等待用户决定。

## TC-37 非页面需求不提供页面提示词选项

- 测试编号：TC-37
- 测试输入：一个纯 API 异步批处理需求的正式方案已经完成，用户尚未决定是否需要能力复盘。
- 预期模式：标准或深度。
- 预期行为：只询问“生成产品经理能力复盘 / 暂不生成”，不提供 Codex 交互页面提示词选项。
- 是否存在 P0：不存在。
- 不应发生的行为：因为系统支持提示词就虚构管理页面；提供不适用的页面提示词选择；自动附加复盘。
- 预期输出：正式 API 方案加一次适用的复盘选择。

## TC-38 用户已决定后不重复询问

- 测试编号：TC-38
- 测试输入：“需求方案完成后直接生成 Codex 交互页面提示词，不要产品经理能力复盘。”
- 预期模式：按主体需求复杂度判断。
- 预期行为：需求无 P0 后直接按用户已确认的范围生成正式方案和提示词，不重复提供四选一，也不输出复盘。
- 是否存在 P0：按主体需求判断。
- 不应发生的行为：重复询问是否需要提示词或复盘；违背“不要复盘”；把已确认的交付选择重新标记为 P0。
- 预期输出：正式需求方案及 Codex 交互页面提示词，不附加复盘或后续选择。
